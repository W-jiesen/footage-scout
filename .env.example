# Copy to .env and fill in. Stock providers need free keys (see README).
# Archival providers (archive.org, wikimedia) need no key.
PEXELS_API_KEY=
PIXABAY_API_KEY=
# Optional global proxy (archive.org & wikimedia are blocked on some networks):
# HTTPS_PROXY=http://127.0.0.1:7890
